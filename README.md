# How to run detox test

```
export ANDROID_SDK_ROOT=/Users/wilson/Library/Android/sdk/
yarn detox:build
yarn detox:test
```

# How to run ios

```
sudo gem install cocoapods
cd ios && pod install && cd ..
yarn ios
```
